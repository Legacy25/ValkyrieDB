#ifndef CDB_CONTEXT_H
#define CDB_CONTEXT_H

/**
 * A class like Context like in the paper can be used to
 * keep track of the runtime state and data in C land for
 * llvm to use, like schemas and the data materialized in
 * that schema, selection parameters, etc.
 */
class Context {

};

#endif //CDB_CONTEXT_H
